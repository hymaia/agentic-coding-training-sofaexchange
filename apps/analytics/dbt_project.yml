name: 'sofaexchange_analytics'
version: '1.0.0'
config-version: 2

profile: 'sofaexchange'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sofaexchange_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
